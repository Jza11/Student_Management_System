# STUDENT MANAGEMENT SYSTEM

The project is a management system dedicated to contain all the necessary data for students. The data will be stored inside ``JSON files`` using **nlohman library**.

The project is built by CMake and was tested using C++20


This is mostly a project to understand the key concepts of using JSON files inside C++ by the functionalities given by the **nlohman library**